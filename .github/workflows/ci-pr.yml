name: CI

# The PR dispatcher uses the workflow from this PR so CI fixes run before merge.
# It still runs Linux AMD64 and Windows jobs on Namespace, including fork PRs.
on:
  pull_request:

permissions: read-all

jobs:
  run:
    uses: ./.github/workflows/ci.yml
